add_executable(tekla-db1-inspect inspect.cpp)
target_link_libraries(tekla-db1-inspect PRIVATE tekla_db1::core)
tekla_db1_set_project_warnings(tekla-db1-inspect)
tekla_db1_enable_sanitizers(tekla-db1-inspect)

install(TARGETS tekla-db1-inspect RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})

add_executable(tekla-db1-inventory inventory.cpp)
target_link_libraries(tekla-db1-inventory PRIVATE tekla_db1::core)
tekla_db1_set_project_warnings(tekla-db1-inventory)
tekla_db1_enable_sanitizers(tekla-db1-inventory)
install(TARGETS tekla-db1-inventory RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})

if(TARGET tekla_db1_gltf)
  add_executable(tekla-db1-glb glb.cpp)
  target_link_libraries(tekla-db1-glb PRIVATE tekla_db1::gltf)
  tekla_db1_set_project_warnings(tekla-db1-glb)
  tekla_db1_enable_sanitizers(tekla-db1-glb)
  install(TARGETS tekla-db1-glb RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
endif()

if(TEKLA_DB1_ENABLE_OCCT)
  add_executable(tekla-db1-occt-worker occt_worker.cpp)
  target_include_directories(tekla-db1-occt-worker PRIVATE ${PROJECT_SOURCE_DIR}/src)
  target_link_libraries(tekla-db1-occt-worker PRIVATE tekla_db1::core)
  tekla_db1_set_project_warnings(tekla-db1-occt-worker)
  tekla_db1_enable_sanitizers(tekla-db1-occt-worker)
  install(TARGETS tekla-db1-occt-worker RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
endif()
